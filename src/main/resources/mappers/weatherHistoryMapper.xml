<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 <mapper namespace="com.quzi.weather_history.mapper.WeatherHistoryMapper">
 	<!-- select -->
	<select id="selectWeatherHistoryList" resultType="com.quzi.weather_history.domain.WeatherHistory">
		SELECT
			`id`
			, `date`
			, `weather`
			, `temperatures`
			, `precipitation`
			, `microDust`
			, `windSpeed`
			, `createdAt`
			, `updatedAt`
		FROM
			`weather_history`
	</select>
 	<!-- insert -->
 	<insert id="insertWeather" parameterType="com.quzi.weather_history.domain.WeatherHistory">
 		INSERT INTO `weather_history`
 		(
 			`date`
			, `weather`
			, `temperatures`
			, `precipitation`
			, `microDust`
			, `windSpeed`
			, `createdAt`
			, `updatedAt`
 		)
 		VALUES
 		(
 			#{date}
 			, #{weather}
 			, #{temperatures}
 			, #{precipitation}
 			, #{microDust}
 			, #{windSpeed}
 			, NOW()
 			, NOW()
 		)
 	</insert>
 </mapper>